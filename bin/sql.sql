CREATE DATABASE IF NOT EXISTS `eatso`;

USE `eatso`;

CREATE TABLE IF NOT EXISTS `catalog` (json LONGTEXT NOT NULL);

CREATE TABLE IF NOT EXISTS `incoming` (json LONGTEXT NOT NULL);

CREATE TABLE IF NOT EXISTS `mugs` (json LONGTEXT NOT NULL);